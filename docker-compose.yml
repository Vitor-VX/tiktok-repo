services:
  tiktok-bot:
    build: .
    container_name: tiktok-bot
    restart: unless-stopped
    volumes:
      - profiles-data:/app/profiles
    ports:
      - "6080:6080"
    environment:
      - TZ=America/Sao_Paulo
    stdin_open: true
    tty: true

volumes:
  profiles-data:
    name: profiles-data
